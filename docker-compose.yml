services:
  salo-birdnet:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - /home/erik/BirdNET-Pi/scripts/birds.db:/data/birds.db:ro
      - /home/erik/BirdSongs:/data/birdsongs:ro
      - /home/erik/BirdNET-Pi/birdnet.conf:/data/config/birdnet.conf:ro
    environment:
      - BIRDNET_DB_PATH=/data/birds.db
      - BIRDNET_RECS_DIR=/data/birdsongs
      - BIRDNET_CONF_PATH=/data/config/birdnet.conf
    restart: unless-stopped
